# TODO: Add Coming Soon Modal for "Thi kiểm tra" Button

1. Edit CourseContentPage.jsx:
   - Import necessary hooks (useState).
   - Add state for modal visibility.
   - Change "Thi kiểm tra" link to a button with onClick handler.
   - Add modal JSX with "Coming Soon" message.

2. Edit CourseContentPage.css:
   - Add styles for modal overlay, content, close button.
   - Make it beautiful: centered, gradient background, animations, responsive.

3. Test the interface:
   - Run the app and navigate to http://localhost:3000/course/9.
   - Click "Thi kiểm tra" to see the modal.
   - Ensure it looks good and closes properly.
